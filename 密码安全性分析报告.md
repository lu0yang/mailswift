# MailSwift 密码安全性分析报告

## 一、系统概述

MailSwift 邮件发送工具涉及两类密码：
- **登录密码**：用户登录系统时验证身份
- **发件邮箱密码**：连接 Exchange EWS 发送邮件

这两种密码的安全需求本质不同，采用了不同的加密策略。

---

## 二、密码存储与加密技术

### 2.1 登录密码 — PBKDF2-SHA256 单向哈希

| 项目 | 参数 |
|------|------|
| 算法 | PBKDF2-HMAC-SHA256 |
| 盐值 | 随机生成的 32 字节 |
| 迭代次数 | 100,000 次 |
| 输出长度 | 32 字节（256 位） |
| 存储格式 | `hex(salt):hex(hash)` |
| 可逆性 | **不可逆**（单向） |

**验证流程：**

```
登录时输入密码 → 取出数据库中的 salt → 同参数重新计算哈希 → 比对
```

**安全性分析：**

- PBKDF2 是 NIST（美国国家标准与技术研究院）推荐的标准密钥派生算法
- 10 万次迭代有效抵御暴力破解：即使攻击者拿到哈希值，每秒只能尝试约 100~1000 次明文
- 每个用户独立的随机盐值，防止彩虹表攻击和跨用户批量破解
- 即使数据库完全泄露，无法还原出原始密码

**代码位置：** `backend/auth.py` — `hash_password()` / `verify_password()`

### 2.2 发件邮箱密码 — AES-256-GCM 对称加密

| 项目 | 参数 |
|------|------|
| 算法 | AES-256-GCM（认证加密） |
| 密钥派生 | PBKDF2-HMAC-SHA256，60 万次迭代 |
| 密钥来源 | 机器唯一标识文件 `.machine_id` |
| 密钥长度 | 256 位 |
| Nonce | 每次加密随机生成 12 字节 |
| 存储格式 | `base64(nonce + ciphertext)` |
| 可逆性 | **可逆**（必须，因为要传给 EWS） |

**为什么必须是可逆的：**

Exchange EWS 验证需要用户的实际明文密码。将密码哈希后无法还原，无法进行 EWS 认证。因此必须使用可逆加密而非哈希。

**密钥派生链：**

```
.machine_id 文件（随机字符串）
    ↓ PBKDF2-SHA256，60万次迭代，固定盐值
AES-256 密钥（256位）
    ↓
明文密码 → AES-256-GCM 加密 → base64 密文 → 存入数据库

解密时：
数据库密文 → base64 解码 → AES-256-GCM 解密 → 明文密码 → 传给 EWS
```

**代码位置：** `backend/crypto_utils.py` — `encrypt_password()` / `decrypt_password()`

### 2.3 JWT Token — HS256 签名

| 项目 | 参数 |
|------|------|
| 算法 | HMAC-SHA256 |
| 密钥 | 可配置的 JWT_SECRET（环境变量） |
| 有效期 | 默认 24 小时（可配置） |
| 载荷 | user_id、email、过期时间 |

**代码位置：** `backend/auth.py` — `create_jwt()` / `decode_jwt()`

### 2.4 自动登录 API 密钥

| 项目 | 参数 |
|------|------|
| 验证方式 | 字符串比对 |
| 密钥 | 可配置的 AUTO_LOGIN_API_KEY（环境变量） |
| 用途 | 同事系统服务器间调用时的身份验证 |

**代码位置：** `backend/config.py` — `AUTO_LOGIN_API_KEY`

---

## 三、安全机制

### 3.1 防御措施

| 措施 | 说明 |
|------|------|
| 单向哈希 | 登录密码不可逆，数据库泄露不会泄露登录凭证 |
| 随机盐值 | 防止彩虹表攻击，相同密码不同用户哈希不同 |
| 高迭代次数 | 登录密码 10 万次，发件密码密钥 60 万次，增加破解成本 |
| 机器绑定 | 发件密码密钥与服务器绑定，数据库单独窃取无法解密 |
| 认证加密 | AES-GCM 同时提供机密性和完整性验证，防止密文篡改 |
| JWT 过期 | Token 24 小时后自动失效，减少泄露窗口 |
| 密钥可配置 | JWT_SECRET 和 API_KEY 通过环境变量注入，不写死在代码中 |

### 3.2 密码在内存中的生命周期

```
登录密码：浏览器输入 → HTTPS POST → 后端 PBKDF2 哈希 → 对比 → 立即丢弃原文
发件密码：浏览器输入 → HTTPS POST → AES-GCM 加密存库 → 发送时解密 → 传给 EWS → 丢弃
```

---

## 四、潜在隐患及风险分析

### 4.1 高风险

| 隐患 | 严重程度 | 说明 | 缓解建议 |
|------|:--:|------|------|
| **发件密码可逆** | ⚠️ 中 | 加密存储，理论上可解密，这是业务需求导致的必然风险 | 加固服务器安全，限制文件访问权限 |
| **传输层未加密** | ⚠️ 高* | 当前使用 HTTP，密码在网络中明文传输 | 部署时启用 HTTPS，或确保仅内网使用 |

> *如果只在内网使用且防火墙限制外部访问，风险降为低。如果暴露公网则需立即配置 HTTPS。

### 4.2 中风险

| 隐患 | 严重程度 | 说明 | 缓解建议 |
|------|:--:|------|------|
| **固定盐值** | ⚠️ 低 | 发件密码密钥派生使用固定盐值 `mail-tool-fixed-salt-v1`，减低了安全性 | 改为随机盐值并存储在数据库中 |
| **.machine_id 文件** | ⚠️ 低 | 密钥存储在同一台服务器上，物理/系统入侵时面临风险 | 文件权限设为 600；定期轮换密钥 |
| **JWT 无刷新机制** | ⚠️ 低 | Token 有效期内无法主动撤销 | 如需增强，可引入 Token 黑名单机制 |
| **密码复杂度无校验** | ⚠️ 低 | 未限制密码长度/复杂度 | 前端增加密码强度提示 |

### 4.3 低风险

| 隐患 | 严重程度 | 说明 |
|------|:--:|------|
| Auto-Login API Key 不可轮换 | 低 | 密钥存储在环境变量，无自动轮换机制 |
| 登录无失败次数限制 | 低 | 暴力破解理论上可行，但 PBKDF2 高迭代次数使得破解极慢 |

---

## 五、攻击场景分析

### 场景 1：数据库完全泄露

| 能获取 | 不能获取 |
|------|------|
| 用户邮箱列表 | 登录密码（PBKDF2 不可逆） |
| 密码哈希值（破解极慢） | 发件密码（缺少 .machine_id 无法解密） |
| 发送历史记录 | |

**结论**：数据库泄露影响有限，攻击者无法直接使用泄露数据登录系统或发送邮件。

### 场景 2：数据库 + .machine_id 同时泄露

| 能获取 | 不能获取 |
|------|------|
| 所有发件邮箱密码 | 登录密码（仍不可逆） |
| 可模拟任意用户发邮件 | 无法修改已发送的历史记录 |

**结论**：服务器完全沦陷的极端场景，此时攻击者已有系统级权限，任何加密方案都无法防范。应重点防护服务器安全。

### 场景 3：网络抓包

| 条件 | 能获取 |
|------|------|
| HTTP（当前） | 密码明文可见 |
| HTTPS | 不可见 |

**结论**：内网部署时风险低；若暴露公网需配置 HTTPS。

---

## 六、部署安全建议

| 优先级 | 建议 | 操作 |
|:--:|------|------|
| **P0** | 配置 HTTPS | 使用公司内部 CA 签发证书，或使用反向代理（Nginx）+ Let's Encrypt |
| **P1** | 修改默认密钥 | JWT_SECRET 和 AUTO_LOGIN_API_KEY 换成随机字符串，至少 32 位 |
| **P1** | 保护 .machine_id | `chmod 600 backend/.machine_id`，仅应用用户可读 |
| **P2** | 数据库最小权限 | mailswift_app 账号仅授予 SELECT/INSERT/UPDATE/DELETE/CREATE/ALTER，回收 DROP/FILE/SUPER 权限 |
| **P2** | 固定盐值动态化 | 将 `mail-tool-fixed-salt-v1` 改为随机生成并存入数据库 |
| **P3** | 登录失败限制 | 同一 IP 5 次失败后锁定 15 分钟 |
| **P3** | Token 撤销机制 | 引入 Redis 黑名单，支持管理员主动撤销 Token |

---

## 七、安全评分

| 维度 | 评分 | 说明 |
|------|:--:|------|
| 密码存储 | ★★★★☆ | 登录密码 PBKDF2 安全；发件密码 AES-GCM 可逆但必要 |
| 传输安全 | ★★☆☆☆ | 当前 HTTP，内网可用但建议加强 |
| 密钥管理 | ★★★☆☆ | 机器绑定尚可，固定盐值可优化 |
| 访问控制 | ★★★★☆ | JWT 认证 + 路由守卫 + API 权限校验 |
| 审计追溯 | ★★★☆☆ | 有发送记录，但缺少登录日志 |

**综合评分：内网工具场景下安全等级良，公网部署需补充 HTTPS。**

---

*报告生成日期：2026-06-16*
*分析版本：MailSwift commit 2256bee*
